<!-- Light-mode variant of logo.svg: same mark, no dark background box (the
     navbar/page background is already light, so a transparent background
     reads cleaner than a boxed icon). Wired up via themeConfig.navbar.logo
     `src` in docusaurus.config.ts; logo.svg itself is used as `srcDark`. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <path d="M 22 28 L 40 72 L 52 72 L 34 28 Z" fill="#38BDF8" />
  <path d="M 48 72 L 62 28 L 70 28 L 56 72 Z" fill="#38BDF8" />
  <path d="M 64 72 L 78 28 L 86 28 L 72 72 Z" fill="#0284C7" />
</svg>
